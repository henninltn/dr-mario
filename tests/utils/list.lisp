(defpackage dr-mario-test.utils.list
  (:use :cl
   :dr-mario.utils.list
        :prove))
(in-package :dr-mario-test.utils.list)

;; NOTE: To run this test file, execute `(asdf:test-system :dr-mario)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
