(defpackage dr-mario-test.utils.macros
  (:use :cl
   :dr-mario.utils.macros
        :prove))
(in-package :dr-mario-test.utils.macros)

;; NOTE: To run this test file, execute `(asdf:test-system :dr-mario)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
